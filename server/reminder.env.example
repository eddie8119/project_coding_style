# 複製此文件為 .env 並填入相應的值

# Supabase 配置
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_SUPABASE_ANON_KEY

# 測試用戶 ID (用於測試腳本)
TEST_USER_ID=your_test_user_id
TEST_PROJECT_ID=your_test_project_id

# LINE Notify 配置 (可選，如果用戶在 UserSettings 表中設置了 token，則不需要)
LINE_NOTIFY_TOKEN=your_line_notify_token

# 提醒排程配置
# 每 5 分鐘檢查一次 LINE 提醒
CHECK_LINE_REMINDERS_CRON="*/5 * * * *"
# 每天 00:00 發送電子郵件摘要
DAILY_EMAIL_DIGEST_CRON="0 0 * * *"

# 電子郵件配置 (用於發送每日摘要)
# 常見的 SMTP 服務:
# Gmail: smtp.gmail.com (需要應用專用密碼)
# Outlook/Hotmail: smtp.office365.com
# Yahoo: smtp.mail.yahoo.com
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false  # 設置為 true 表示使用 SSL/TLS (通常用於 port 465)
EMAIL_USER=your_email_user
EMAIL_PASSWORD=your_email_password
EMAIL_FROM=noreply@example.com
EMAIL_FROM_NAME="任務提醒系統"  # 發件人名稱
